<%= @posts.each do |p| %>
    <p>
        <ul>
            <li><%= p.title %></li>
            <li><%= p.description %></li>
            <li><%= p.votes.count %></li>
            <li><%= p.comments.first.description %></li>
        </ul>
    </p>
<% end %>