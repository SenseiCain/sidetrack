<%= @reference_user.name %>

<% if @posts %>
    <div>
        <p>Posts</p>
        <% @posts.each do |p| %>
            <%= display_post_tag(p, @vote, p.user) %>
        <% end %>
    </div>
<% end %>